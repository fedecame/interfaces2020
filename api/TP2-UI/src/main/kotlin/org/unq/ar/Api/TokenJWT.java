package org.unq.ar.Api;
/*
class UserGenerator: JWTGenerator<User>{
    override fun generate(user: User, algorithm: Algorithm):String{
        val token = JWT.create().with
        return token.sign(algorithm)
        }
        }

public class TokenJWT {

    var Algorithm algorithm = Algorithm.HMAC256("very_secret");

    JWTGenerator<MockUser> generator = (user, alg) -> {
        JWTCreator.Builder token = JWT.create()
                .withClaim("name", user.name)
              //  .withClaim("level", user.level);
        return token.sign(alg);
    };

   val JWTVerifier verifier = JWT.require(algorithm).build();
   val JWTProvider provider = JWTProvider(algorithm, generator, verifier);
}*/
